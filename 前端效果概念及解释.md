# 一、鼠标滚动时的效果展示

这是三个独立机制叠在一起的效果，我逐一拆解（都在 `web/src/App.tsx`、`web/src/scene/Scene.tsx`、`web/src/ui/Works.tsx` 里）：

## 1️⃣ 内容正常往上滚 + 背景固定

```
/* styles.css */
.scene-bg { position: fixed; inset: 0; z-index: 0; pointer-events: none; }
```

3D 场景是 `position: fixed` 的全屏 Canvas，**钉在视口不随文档滚动**；`<main class="content">` 里的履历/作品是普通文档流。所以"内容往上滚"就是普通滚动，背景本身不动。

## 2️⃣ 背景"波动" = 滚动在"播放"GLB 自带的相机动画

背景不是物理视差，而是**用滚动位置去 seek 一条相机关键帧动画**（时间轴 scrub）：

```
// Scene.tsx —— 核心：滚动位置 → 相机动画帧
let sTarget = clamp(frameSmooth.current / FRAMES_PER_NODE - 1, -1, M - 1)
// 参考线在视口 30% 高度：履历条目标题过线即切到下一个节点
const refLine = window.scrollY + window.innerHeight * NODE_LINE
// ...
let frameTarget = clamp((sTarget + 1) * FRAMES_PER_NODE, 0, RESUME_FRAMES)
// 每帧把 mixer 拨到对应帧 → 镜头就位
for (const { action, duration } of actions.current) {
  act.time = Math.min(t, duration)
}
mixer.update(0)
```

- 镜头运动是美术在 Blender/intro3d 里做好的 **`CameraAction` clip（100 帧 @24fps）**，模型文件里就带着
- `Scene.tsx` 每帧读滚动位置：每条履历条目顶部越过视口 30% 参考线 → 切到下一个"节点"，对应到动画帧 `(s+1)×50`
- 帧再经过 smoothstep 缓动，所以镜头是**平滑运镜**而非跳变——这就是你看到的"背景波动"
- 进入作品区后时间轴继续延伸（`RESUME_FRAMES → totalFrames`），镜头还会继续横移运镜

## 3️⃣ 到底后往下滚 → 从右往左：sticky 横向画廊

作品区用的是经典 **"竖向滚动 pin 转横向位移"（sticky horizontal scroll）**：

```
// Works.tsx
const { scrollYProgress } = useScroll({ target: galleryRef, offset: ['start start', 'end end'] })
const x = useTransform(scrollYProgress, [0, 1], [0, -scrollRange]) // 竖滚进度 → 横向位移
...
<div className="wk-gallery" style={{ height: `calc(100vh + ${scrollRange}px)` }}>
  <div className="wk-gallery-sticky">   {/* position: sticky; top: 0 */}
    <motion.div className="wk-track" style={{ x }}>
.wk-gallery { position: relative; }
.wk-gallery-sticky { position: sticky; top: 0; height: 100vh; overflow: hidden; }
.wk-track { display: flex; padding-left: 100vw; will-change: transform; }
```

原理三步：

1. **制造额外滚动距离**：`.wk-gallery` 高度 = `100vh + scrollRange`（`scrollRange = track.scrollWidth - 视口宽`），人为多出一段可滚动的竖向空间
2. **钉住视口**：`.wk-gallery-sticky` 用 `position: sticky; top: 0`，在这段滚动距离里始终占满视口、不走位
3. **竖滚映射横移**：framer-motion 的 `useScroll` 量出这段竖向滚动进度 0→1，`useTransform` 把它变成 track 的 `x: 0 → -scrollRange`，于是**滚轮往下 → 卡片排整体向左平移**

`padding-left: 100vw` 让第一张卡片初始位于右视口外，滚动时从右侧依次滑入——这就是"从右往左滚"的观感；右下角还有 `wk-progress` 进度条 `scaleX: scrollYProgress` 同步显示进度。

# 二、技术使用

| 技术     | 作用                     |
| -------- | ------------------------ |
| Blender  | 做人物、模型、动画       |
| Three.js | 网页显示 3D              |
| Vue3     | 网站整体 UI              |
| GSAP     | 页面动画、镜头动画       |
| GLTF/GLB | Blender → Web 的模型格式 |
| HDRI     | 环境光/反射              |
| WebGL    | 浏览器底层 3D 渲染       |