# 当前项目的前端效果说明

本项目不是把 3D 作为单独背景视频播放，而是将滚动、GLB 动画、景深和 HTML 内容映射到同一条阅读节奏中。核心实现位于 `web/src/App.tsx`、`web/src/scene/Scene.tsx` 与 `web/src/ui/Works.tsx`。

## 固定 3D 场景与滚动内容

`App.tsx` 中的 Canvas 通过 `.scene-bg { position: fixed; inset: 0; }` 固定在视口内。About、履历和作品集位于普通文档流中，因此浏览器处理的是正常的纵向滚动，而 3D 场景保持在背后。

页面额外叠加了滚动渐暗、右侧磨砂轨道、首屏画框与胶片噪点。这些层不接收指针事件，不会影响页面阅读和工具操作。

## 滚动如何驱动运镜

模板优先读取 GLB 内名为 `CameraAction` 的动画。`Scene.tsx` 根据首屏、履历锚点和作品区的位置计算目标帧，再让 Three.js 的 `AnimationMixer` 跳转到该时间点：

```text
页面滚动位置
  -> 履历 / 作品区的 DOM 位置
  -> GLB 动画帧
  -> 相机位置、焦点和后期效果
```

`focus-start`（或 `focus-0`）、`focus-1` 到 `focus-5`、`focus-works` 是模型中的焦点空对象。它们与 `src/data/focusPoints.ts` 的列表共同决定履历节点的镜头停靠位置。

## 自定义运镜

开发模式下，左下角工具菜单的“运镜”入口提供两种模式：

- **预设 1**：完整使用 GLB 内置的 `CameraAction`。
- **自定义**：冻结 `CameraAction` 在起始状态，随后由可视化关键帧叠加镜头偏移、焦点偏移、FOV、虚化强度和清晰范围。

关键帧写入 `web/public/director/camera-overrides.json`。因此即便导入的 GLB 缺少模板的相机动画，也能在统一的场景坐标中完成基本运镜。

## 焦点与景深

后期顺序是 `DepthOfField -> Bloom -> SMAA`。景深焦点每帧跟随当前焦点锚点；有些 GLB 可以在节点的 `userData` 中携带独立的虚化和焦点范围，模板会优先使用这些值。自定义关键帧同样可以覆盖 FOV、虚化强度和清晰范围。

## 作品区的横向画廊

作品区仍使用纵向滚动输入，但内部画廊通过 `position: sticky` 固定一屏，再把纵向滚动进度映射为横向 `x` 位移。用户持续向下滚动，作品卡片从右向左进入；3D 镜头在同一段时间里继续推进。

## 统一工具菜单与资料编辑

左下角只有一个磨砂工具按钮，向上展开贴纸、运镜和资料三个入口。进入任一编辑器后菜单会收起，避免与右侧编辑面板或资料弹窗重叠。

资料编辑器把当前页面内容保留为“预设 1”，并将自定义信息保存至 `web/public/profile/profile.json`。保存后的资料在首屏四角元数据和 About 区域即时渲染。

## 为什么编辑器只在本地开发模式显示

工具会直接写入 JSON、配置和 GLB 文件，依赖 Vite 开发服务器提供的本地接口。`npm run build` 生成的是静态网站，不能也不应该让访客直接覆盖仓库文件。

推荐流程是：本地编辑 -> 保存 -> `npm run build` -> 提交配置与素材 -> 部署 `web/dist/`。
